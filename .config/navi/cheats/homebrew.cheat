% brew homebrew

# Remove homebrew tap
brew untap <TAP>

# List installed taps with details
brew tap-info --installed

# List installed taps
brew tap

# List/search available packages in tap
brew search <TAP>

$ TAP: brew tap
