############################
#          Repos           #
############################

# Kopia
key https://kopia.io/signing-key
deb "[arch=amd64,arm64,armhf] https://packages.kopia.io/apt/ stable main"

############################
#           Core           #
############################

apt apt-transport-https
apt bubblewrap
apt build-essential
apt ca-certificates
apt git
apt make
apt openssl
apt ssh
apt unzip
apt vim

############################
#       Development        #
############################

apt binutils
apt direnv
apt jq
apt kubecolor
apt kubectx
apt libbz2-dev
apt libffi-dev
apt liblzma-dev
apt libncurses-dev
apt libnetfilter-queue-dev
apt libpcap-dev
apt libreadline-dev
apt libsqlite3-dev
apt libssl-dev
apt libusb-1.0-0-dev
apt libxml2-dev
apt libxmlsec1-dev
apt nodejs
apt npm
apt pipx
apt python3-dev
apt python3-pip
apt tk-dev
apt xz-utils
apt yamllint
apt zlib1g-dev

############################
#       Python Tools       #
############################

apt ansible
apt black
apt isort
apt jc
apt pre-commit
apt python3
apt python3-bs4
apt python3-jmespath
apt python3-loguru
apt python3-requests
apt python3-rich
apt python3-venv
apt tldr

############################
#        Utilities         #
############################

apt fzf
apt htop
apt iotop
apt makepasswd
apt moreutils
apt p7zip-full
apt pv
apt pwgen
apt screen
apt tmux
apt tree

############################
#         Security         #
############################

apt nmap
apt secure-delete
apt wipe

############################
#         Network          #
############################

apt bridge-utils
apt conntrack
apt ethtool
apt socat
apt traceroute
apt wireguard

############################
#         Storage          #
############################

apt kopia
apt rclone
apt smartmontools

############################
#     Virtualization       #
############################

apt postgresql-client
apt schroot

############################
#      Observability       #
############################

apt sysstat
